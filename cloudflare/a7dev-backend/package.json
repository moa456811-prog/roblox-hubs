{
  "name": "a7dev-cloudflare-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:init:local": "wrangler d1 execute a7dev-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute a7dev-db --remote --file=schema.sql",
    "test": "node test/cutover.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}

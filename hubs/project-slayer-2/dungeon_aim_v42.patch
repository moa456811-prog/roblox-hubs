--- base-v41
+++ base-v42
@@ -3178,39 +3178,8 @@
 
 -- One reusable player constraint; the existing heartbeat owns its lifetime.
 State.DungeonHover = {}
--- Local target geometry only; server damage validation remains authoritative.
-State.DungeonHover.restoreHitbox = function()
-    local h = State.DungeonHover
-    local entry = h.Hitbox
-    h.Hitbox = nil
-    if not entry then return end
-    pcall(function()
-        local part = entry.Root
-        if part.Size == entry.AppliedSize then part.Size = entry.Size end
-        if part.CanCollide == false then part.CanCollide = entry.CanCollide end
-        if part.Massless == true then part.Massless = entry.Massless end
-    end)
-end
-State.DungeonHover.applyHitbox = function(model, part)
-    local h = State.DungeonHover
-    if h.Hitbox and h.Hitbox.Root == part then return end
-    h.restoreHitbox()
-    if not aliveModel(model) or isPlayerCharacter(model)
-        or not part or not part.Parent or not part:IsA("BasePart") then return end
-    local size = part.Size
-    local entry = {Root=part,Size=size,CanCollide=part.CanCollide,Massless=part.Massless,
-        AppliedSize=Vector3.new(math.max(size.X,10),math.max(size.Y,14),math.max(size.Z,10))}
-    h.Hitbox = entry
-    local ok = pcall(function()
-        part.CanCollide = false
-        part.Massless = true
-        part.Size = entry.AppliedSize
-    end)
-    if not ok then h.restoreHitbox() end
-end
 State.DungeonHover.stop = function()
     local h = State.DungeonHover
-    h.restoreHitbox()
     for _, key in ipairs({"Position", "Orientation", "Attachment"}) do
         local object = h[key]
         h[key] = nil
@@ -3269,7 +3238,6 @@
         hum.AutoRotate = false
     end
     h.LastTouch = now
-    h.applyHitbox(model, targetRoot)
     local changed = h.Target ~= model
     if not changed and now < (h.NextUpdate or 0) then return true end
     h.NextUpdate = now + Config.DungeonAboveMoveInterval
@@ -3286,7 +3254,7 @@
     end
     local goal = targetRoot.Position + Vector3.new(0, Config.DungeonAboveHeight, 0)
         + h.Approach * Config.DungeonAboveBackOffset
-    local pose = CFrame.lookAt(goal, goal - h.Approach)
+    local pose = CFrame.lookAt(goal, targetRoot.Position)
     h.Position.MaxForce = math.max(10000, root.AssemblyMass * (workspace.Gravity + 500) * 10)
     if changed or (h.Position.Position - goal).Magnitude > 0.25 then
         h.Position.Position = goal
@@ -9631,7 +9599,9 @@
         and now - (State.SmartFarmLastReposition or 0) >= Config.FarmRepositionDelay then
         State.SmartFarmLastReposition = now
         local _, _, myRoot = getCharacter()
-        if myRoot then
+        -- Hover owns movement; the legacy recovery teleport fights its constraint.
+        if myRoot and not (State.DungeonHover.Root == myRoot
+            and State.DungeonHover.Target == target) then
             local side = (State.SmartFarmStuckRecoveries or 0) % 2 == 0 and 1 or -1
             local pos = root.Position
                 + root.CFrame.RightVector * (Config.FarmDistance * side)
@@ -12793,7 +12763,7 @@
             end
         end)
         State.Runtime.addInfo(dungeonSoul,"Auto Clear: locks one mob until death, then switches instantly")
-        State.Runtime.addInfo(dungeonSoul,"Combat: 7-stud hover | enlarged local target hitbox")
+        State.Runtime.addInfo(dungeonSoul,"Combat: 7-stud hover | aim toward target")
         State.Runtime.addToggle(dungeonSoul,"Auto Pick Dungeon Cards",State.Flags.AutoDungeonCards==true,function(v)
             State.Flags.AutoDungeonCards=v
             State.DungeonLastCardPick=0

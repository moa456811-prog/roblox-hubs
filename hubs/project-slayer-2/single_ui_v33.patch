--- base-v32
+++ base-v33
@@ -11493,20 +11493,23 @@
 ENV.A7DEV_PROJECT_SLAYER_2 = State
 
 buildHub = function()
-    if State.Destroyed then
-        return
-    end
-
-    local old = PlayerGui:FindFirstChild("A7DEV_ProjectSlayer2")
-    if old then
-        old:Destroy()
+    if State.Destroyed or State.UIBuilding then return end
+    if State.Gui and State.Gui.Parent then return end
+    State.UIBuilding=true
+
+    for _,oldGui in ipairs(PlayerGui:GetChildren()) do
+        if oldGui:IsA("ScreenGui") and oldGui.Name=="A7DEV_ProjectSlayer2" then
+            oldGui:Destroy()
+        end
     end
 
     initNative()
     startNPCRegistry()
+    if State.Destroyed then State.UIBuilding=false; return end
 
     local gui = Instance.new("ScreenGui")
     gui.Name = "A7DEV_ProjectSlayer2"
+    gui.Enabled = false
     gui.ResetOnSpawn = false
     gui.IgnoreGuiInset = false
     gui.DisplayOrder = 999980
@@ -13067,6 +13070,8 @@
 
     setStatus(State.CombatReady and "Ready | native combat linked" or "Ready | native combat not found")
     notify("A7DEV // Project Slayer 2", "Runtime Lock loaded", 4)
+    State.UIBuilding=false
+    gui:SetAttribute("A7DEVBaseReady",true)
 end
 
 -- Start auth or main hub. Saved keys are revalidated silently.

--- runtime-v28
+++ runtime-v33
@@ -3664,7 +3664,7 @@
 for _=1,1200 do
     if not P.alive then return end
     State=ENV.A7DEV_PROJECT_SLAYER_2
-    if State then break end
+    if State and not State.Destroyed then break end
     task.wait(.25)
 end
 if not State then return end
@@ -3685,14 +3685,18 @@
 local pg=LocalPlayer:WaitForChild("PlayerGui")
 for _=1,1200 do
     if not P.alive then return end
-    gui=pg:FindFirstChild("A7DEV_ProjectSlayer2")
-    if gui then break end
+    if State.Destroyed or ENV.A7DEV_PROJECT_SLAYER_2~=State then return end
+    gui=State.Gui
+    if gui and gui.Parent and gui:GetAttribute("A7DEVBaseReady")==true then break end
+    gui=nil
     task.wait(.25)
 end
 if not gui then return end
 
-task.wait(.35)
-pcall(installLayout, gui, State)
+if not P.alive or State.Destroyed or ENV.A7DEV_PROJECT_SLAYER_2~=State then return end
+local layoutOK,layoutError=pcall(installLayout, gui, State)
+if not layoutOK then warn("[A7DEV] UI layout: "..tostring(layoutError)) end
+gui.Enabled=true
 pcall(installRestoredFeatures, State, gui)
 native()
 for _,x in ipairs(gui:GetDescendants()) do
